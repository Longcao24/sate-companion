apply plugin: 'com.android.library'
apply plugin: 'expo-module-gradle-plugin'

group = 'expo.modules.satefgservice'
version = '0.1.0'

android {
  namespace "expo.modules.satefgservice"

  defaultConfig {
    versionCode 1
    versionName "0.1.0"
  }

  lint {
    abortOnError false
  }
}
