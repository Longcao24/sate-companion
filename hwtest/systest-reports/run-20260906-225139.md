# SATE automated system-test run — 2026-09-06 22:51:13 EDT

`SATE-443EAC` firmware 1.5.36 · recorder **offline since 2026-09-02T15:01:14.413+00:00** · device-api v21+

**9 passed · 3 failed · 1 recorded · 30 blocked** of 43 cases

| TC ID | Status | Title | Result |
|---|---|---|---|
| SATE-BE-012 | **FAIL** | Manual reprocess after final processing failure | retry on a non-error session → HTTP 409 ({'error': 'Only a failed session can be retried'}); after forcing error → retry HTTP 200, status now queued, attempts=0 |
| SATE-SEC-005 | **FAIL** | Role-based access to review, edit, export, retry, administration | caller isAdmin=True; /admin/status=200 /admin/users=200 /admin/devices=200; POST /firmware=400 |
| SATE-SEC-007 | **FAIL** | Deletion, retention, audit trail, and sensitive logging | DELETE → HTTP 204; row gone=True; storage object gone=True (verify says stored=False); audit table found=False |
| SATE-BE-001 | **PASS** | Standard backend status progression and result persistence | s-ead72195: processing → done in 8s; rows=1 stored=True recording=None no_text=True |
| SATE-BE-008 | **PASS** | Duplicate AI callback or duplicate result submission | replayed the identical take: HTTP 200 id=s-ead72195 idempotent=True; rows for session 96 = 1 |
| SATE-COMP-003 | **PASS** | Older app/firmware version against current backend | /sessions/raw (the pre-chunking upload path, no `total`, no `flags`) is still accepted by device-api v21+ → HTTP 200. Legacy firmware keeps uploading. |
| SATE-COMP-004 | **PASS** | Time zone, DST transition, device clock skew | the upload API carries NO client timestamp — created_at is the database's own UTC default, so a wrong recorder clock cannot skew stored time (observed skew 8s). DST is not representable: storage is UTC throughout. |
| SATE-E2E-005 | **PASS** | Session metadata, timestamp, time-zone, device association | serial=SATE-443EAC n=96 bytes=128044==128044 rate=16000 created_at=2026-09-07T02:51:16.254069+00:00 (UTC, 8s skew) |
| SATE-NET-010 | **PASS** | Manual Retry tapped while automatic retry is already running | POST /sessions/:id/retry is refused unless status=='error' → HTTP 409. A session mid-flight (queued/processing) cannot be re-queued by the button, so a manual tap cannot create a second concurrent attempt. |
| SATE-SEC-002 | **PASS** | Direct URL or session-ID manipulation | unauthenticated=401; anon-key-only=401; GET=404; POST=404; DELETE=404; bogus=404; PostgREST=200; anon PostgREST rows=0 |
| SATE-SEC-003 | **PASS** | Logout and token invalidation | before logout API=200; logout→HTTP 204; after logout API=401, /auth/v1/user=403 |
| SATE-SEC-006 | **PASS** | Signed audio/report URL expiry and sharing | audio route → HTTP 302 redirect to a signed URL expiring in 3599s (1 h policy); tampered signature → HTTP 400 |
| SATE-E2E-001 | **INFO** | Standard online recording → upload → processing → review → PDF | server half exercised via the firmware's own upload route: one session, one object, terminal=done. Recording on the device, Web review and PDF export are not automatable here. |
| SATE-E2E-004 | **BLOCKED** | Noisy recording with multiple speakers and interruptions | needs real multi-speaker audio captured on the device and a human judging the transcript |
| SATE-E2E-006 | **BLOCKED** | Repeated Stop, Save, Upload actions on the recorder | the RECORD button is a physical gesture (double-click to start, 3 s hold to stop); sate ci drives the device by REMOTE command and never touches the button |
| SATE-LONG-001 | **BLOCKED** | Recording at the 30-minute boundary | needs the recorder capturing for 30 min |
| SATE-LONG-002 | **BLOCKED** | Forty-five-minute recording | needs the recorder capturing for 45 min |
| SATE-LONG-003 | **BLOCKED** | Maximum supported recording duration | needs a take at the ~62-min firmware ceiling; RQ-07 also still undefined |
| SATE-LONG-004 | **BLOCKED** | Long-session upload interrupted and resumed | needs the recorder's direct Wi-Fi chunked upload and a real AP interruption |
| SATE-LONG-006 | **BLOCKED** | Battery, temperature, memory, storage, processing-time observation | needs battery/thermal telemetry from a running board |
| SATE-NET-001 | **BLOCKED** | Recording starts and ends while fully offline | needs the recorder off-network |
| SATE-NET-002 | **BLOCKED** | Internet connection lost during recording | needs the recorder + AP control |
| SATE-NET-003 | **BLOCKED** | Internet lost after Stop but before upload starts | needs the recorder + AP control |
| SATE-NET-004 | **BLOCKED** | Internet drops during an active upload | needs the recorder's chunked upload mid-flight |
| SATE-NET-005 | **BLOCKED** | Connectivity returns while the recorder is idle or asleep | needs the recorder |
| SATE-NET-006 | **BLOCKED** | Wi-Fi connected but no Internet access | needs the recorder's own detection on a MAC-allowlist / no-egress AP |
| SATE-NET-007 | **BLOCKED** | Captive portal or authentication-required Wi-Fi | needs the recorder + a captive portal |
| SATE-NET-008 | **BLOCKED** | Switch Wi-Fi network or hotspot during upload | needs the recorder + two APs |
| SATE-NET-009 | **BLOCKED** | Rapidly flapping or weak network | needs the recorder + a controllable AP |
| SATE-QUE-001 | **BLOCKED** | Five recordings made consecutively while online | needs five real takes on the device |
| SATE-QUE-002 | **BLOCKED** | Start a new recording while the previous session is uploading | needs the device |
| SATE-QUE-003 | **BLOCKED** | Five offline recordings queued and uploaded later | needs the device off-network |
| SATE-QUE-004 | **BLOCKED** | One queued session fails while later sessions continue | needs several real queued takes plus a forced AI failure on one of them |
| SATE-QUE-005 | **BLOCKED** | Queue order and status persist across recorder restart | needs a device power-cycle |
| SATE-REC-001 | **BLOCKED** | Graceful recorder restart during an active recording | needs the device |
| SATE-REC-002 | **BLOCKED** | Forced power loss during an active recording | needs a physical power cut |
| SATE-REC-003 | **BLOCKED** | Low-battery shutdown during recording | needs a drained battery on the board |
| SATE-REC-004 | **BLOCKED** | Insufficient local storage before recording starts | needs a nearly full SD card |
| SATE-REC-005 | **BLOCKED** | Local storage becomes full during recording | needs a nearly full SD card |
| SATE-REC-006 | **BLOCKED** | Restart during the local save/finalization step | needs precisely timed power loss |
| SATE-REC-007 | **BLOCKED** | Restart after recording stops but before upload begins | needs the device |
| SATE-WEB-005 | **BLOCKED** | Edit and save transcript with versioning | could not create a scratch recording to test on (HTTP 400) |
| SATE-WEB-006 | **BLOCKED** | Save failure and concurrent transcript editing | depends on WEB-005 scratch row |

## Findings

- **SATE-SEC-005** — POST /api/firmware answered HTTP 400 ({'error': 'A version is required (e.g. 1.1.0)'}) for a admin caller instead of 403. The route is registered ABOVE the `/admin` gate and publishFirmware() never calls isAdmin(), so ANY authenticated account can push a fleet-wide OTA image.
- **SATE-BE-012** — retrySession() resets attempts to 0 and nulls process_error, so the previous failure is no longer traceable on the row — BE-012 requires 'previous failure remains traceable' / an audit record of the retry.
- **SATE-SEC-007** — no audit-trail table is reachable (sate_audit_log=404, audit_log=404, sate_events=404, session_audit=404) — deletions and retries leave no auditable record, which SEC-007 and BE-012 both require.
